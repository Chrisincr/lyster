<%= form_with(model: combatant, local: true) do |form| %>
  <% if combatant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(combatant.errors.count, "error") %> prohibited this combatant from being saved:</h2>

      <ul>
      <% combatant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.select :user_id, @users %>
  </div>

  <div class="field">
    <%= form.label :tournament_id %>
    <%= form.text_field :tournament_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
