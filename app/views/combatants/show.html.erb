<p>
  <strong>User:</strong>
  <%= @combatant.user.name %>
</p>

<p>
  <strong>Tournament:</strong>
  <%= @combatant.tournament.name %>
</p>
<% if (current_user && current_user.id == @combatant.tournament.user.id) %>
  <%= link_to 'Edit', edit_combatant_path(@combatant), class: 'btn btn-primary'  %> |

<% end %>
<%= link_to 'Back', tournament_path(id:@combatant.tournament_id), class: 'btn btn-primary'  %>