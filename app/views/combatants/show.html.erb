<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @combatant.user.name %>
</p>

<p>
  <strong>Tournament:</strong>
  <%= @combatant.tournament.name %>
</p>

<%= link_to 'Edit', edit_combatant_path(@combatant), class: 'btn btn-primary'  %> |
<%= link_to 'Back', tournament_path(params[:tid]), class: 'btn btn-primary'  %>